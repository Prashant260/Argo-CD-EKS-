# GitHub-hosted Actions runners use changing IPs, so the public EKS API endpoint
# is currently open. Replace this with private endpoint access and a self-hosted
# runner or VPN before treating this as production-ready.
AWS-0040

# EKS managed nodes require unrestricted outbound access for AWS APIs, ECR,
# container image pulls, DNS/NTP, and Kubernetes control-plane communication.
AWS-0104
