This is a local GitOps repository skeleton used for testing `scripts/update-gitops.sh`.

The deployment image is a placeholder `REPLACE_WITH_IMAGE` that the update script replaces with the built ECR image URI.
